name: weather
description: Multi-platform bot that provides weather to the Coop chat plus some extra things
version: 3.5.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dotenv: ^4.0.1
  teledart: ^0.5.4
  html: ^0.15.0
  debounce_throttle: ^2.0.0
  crypto: ^3.0.2
  nyxx: ^4.4.0
  nyxx_commands: ^4.3.0
  cron: ^0.5.1
  args: ^2.4.0
  http: ^0.13.5
  collection: ^1.17.1
  postgres: ^2.6.1
  meta: ^1.9.0
  uuid: ^3.0.7

dev_dependencies:
  analyzer: ^5.6.0
  dart_code_metrics: ^5.6.0
  lints: ^2.0.1
  test: ^1.6.0

dependency_overrides:
  analyzer: ^5.6.0
