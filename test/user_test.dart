import 'package:test/test.dart';
import 'utils/setup.dart';

void main() {
  setupTestEnvironment();

  group('User', () {
    test('test user', () {
      expect(true, equals(true));
    });
  });
}
